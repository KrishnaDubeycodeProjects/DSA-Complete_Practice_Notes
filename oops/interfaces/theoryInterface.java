// Firstly what's a interface

// A Interface is a facility provided by the JAVA8 isme abstract methods hote hain jinka implementation uske
// sabhi implemented class override kartein hai well it is compulsory and default methods are optional in those methods


// now let's talk about some features
// How it is different from abstract classes
    // 1. Multiple inheritance

    // 2.constructor cannot be form since object can't to be form well exactly with abstract class but
    // constructor can be form since it is a class but cannot use for initialize since, it inherited


    // 3.abstract class can be public default protected since it pass using inherit "extends" keyword
    // interface public and default since it has nothing to provided woh toh abstract ke passs bhi nahi hota
    //   par generally , functions jo non abstract hotein hai woh transmit hotein and baaki function toh ovverride
    // ho hi rahe hai


   //4. data variables are strictly to be static and final
   // 5. normal function in interface are strictly to be static
